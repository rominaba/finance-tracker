{
  "name": "finance-tracker-frontend",
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r index.html login.html dashboard.html transactions.html style.css js dist/"
  }
}